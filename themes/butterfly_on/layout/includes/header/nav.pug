//- //- 此文件主要控制：菜单栏的显示渲染
//- if theme.nav.enable
//-   nav#nav
//-     span#blog-info
//-       //- 标题栏栏左侧带有logo的标题按钮
//-       a(href=url_for('/') title=config.title)
//-         if theme.nav.logo
//-           img.site-icon(src=url_for(theme.nav.logo))
//-         if theme.nav.display_title
//-           span.site-name=config.title
      
//-     #menus
//-       //- 菜单栏
//-       !=partial('includes/header/menu_item', {}, {cache: true})
//-       .moblie-menu#moblie-menu
//-         !=partial('includes/header/moblie-menu', {}, {cache: true})
//-   #moblie-menu 
//-     //- include ./moblie-menu.pug

//- //- 此文件主要控制：菜单栏的显示渲染
//- if theme.nav.enable
//-   nav#nav
//-     span#blog-info
//-       //- 标题栏栏左侧带有logo的标题按钮
//-       a(href=url_for('/') title=config.title)
//-         if theme.nav.logo
//-           img.site-icon(src=url_for(theme.nav.logo))
//-         if theme.nav.display_title
//-           span.site-name=config.title
      
//-     #menus
//-       //- 菜单栏
        
//-       if theme.nav.menu.enable
//-         //- 此段主要提供：自定义菜单项的显示
//-         .menus_items
//-           each value, label in theme.nav.menu.item 
//-             //- 遍历菜单项
//-             if typeof value !== 'object'
//-               if value === 'SEARCH_BUTTON'
//-                 if (theme.algolia_search.enable || theme.local_search.enable || theme.docsearch.enable)

//-                   - const valArray = label.split('||')
//-                   //- 搜索按钮
//-                   #search-button
//-                     a.site-page.social-icon.search(href="javascript:void(0);")
//-                       if valArray[1]
//-                         i.fa-fw(class=trim(valArray[1]))
//-                       else 
//-                         i.fas.fa-search.fa-fw
//-                       span=(' ' + valArray[0]) || (' ' + _p('search.title'))
//-                   #toggle-menu
//-                     a.site-page(href="javascript:void(0);")
//-                       i.fas.fa-bars.fa-fw
//-               else
//-                 .menus_item
//-                   - const valueArray = value.split('||')
//-                   a.site-page(href=url_for(trim(valueArray[0])))
//-                     if valueArray[1]
//-                       i.fa-fw(class=trim(valueArray[1]))
//-                     span=' '+label
//-             else if typeof value === 'object'
//-               if value.html 
//-                 //- 自定义HTML菜单项
//-                 .menus_item 
//-                    != value.html
//-               else
//-                 .menus_item
//-                   - const labelArray = label.split('||')
//-                   - const hideClass = labelArray[2] && trim(labelArray[2]) === 'hide' ? 'hide' : ''
//-                   a.site-page.group(class=`${hideClass}` href='javascript:void(0);')
//-                     if labelArray[1]
//-                       i.fa-fw(class=trim(labelArray[1]))
//-                     span=' '+ trim(labelArray[0])
//-                     i.fas.fa-chevron-down
//-                   ul.menus_item_child
//-                     each val,lab in value 
//-                       - const valArray = val.split('||')
//-                       li
//-                         a.site-page.child(href=url_for(trim(valArray[0])))
//-                           if valArray[1]
//-                             i.fa-fw(class=trim(valArray[1]))
//-                           span=' '+ lab

//- //- 此文件主要控制：菜单栏的显示渲染
//- nav#nav
//-   span#blog-info
//-     //- 标题栏栏左侧带有logo的标题按钮
//-     a(href=url_for('/') title=config.title)
//-       if theme.nav.logo
//-         img.site-icon(src=url_for(theme.nav.logo))
//-       if theme.nav.display_title
//-         span.site-name=config.title
    
//-   #menus
//-     //- 菜单栏
//-     if (theme.algolia_search.enable || theme.local_search.enable || theme.docsearch.enable)
      
//-     if theme.nav.menu.enable
//-       if theme.nav.menu.search_placement == 'right'
//-         !=partial('includes/header/menu_item', {}, {cache: true})
//-         #search-button
//-             a.site-page.social-icon.search(href="javascript:void(0);")
//-               i.fas.fa-search.fa-fw
//-               span=' '+_p('search.title')
//-         #toggle-menu
//-           a.site-page(href="javascript:void(0);")
//-             i.fas.fa-bars.fa-fw

//-       if theme.nav.menu.search_placement == 'left'
//-         #toggle-menu
//-           a.site-page(href="javascript:void(0);")
//-             i.fas.fa-bars.fa-fw
//-         #search-button
//-             a.site-page.social-icon.search(href="javascript:void(0);")
//-               i.fas.fa-search.fa-fw
//-               span=' '+_p('search.title')
//-         !=partial('includes/header/menu_item', {}, {cache: true})

nav#nav
  span#blog-info
    a(href=url_for('/') title=config.title)
      if theme.nav.logo
        img.site-icon(src=url_for(theme.nav.logo))
      if theme.nav.display_title
        span.site-name=config.title
    
  #menus
    if (theme.algolia_search.enable || theme.local_search.enable || theme.docsearch.enable)
      #search-button(tag="pcEnable_false")
        a.site-page.social-icon.search(href="javascript:void(0);")
          i.fas.fa-search.fa-fw
          span=' '+_p('search.title')
    !=partial('includes/header/menu_item', {}, {cache: true})

    #toggle-menu
      a.site-page(href="javascript:void(0);")
        i.fas.fa-bars.fa-fw


